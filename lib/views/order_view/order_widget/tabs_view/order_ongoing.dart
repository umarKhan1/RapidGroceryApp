import 'package:flutter/material.dart';


import '../card_widget.dart';

class OnGoinView extends StatelessWidget {
  const OnGoinView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
body: cardView(context: context),

    );
  }
}